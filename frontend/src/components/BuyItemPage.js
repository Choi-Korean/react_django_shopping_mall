import React, { Component } from 'react';

export default class BuyItemPage extends Component{
    constructor(props){
        super(props);
    }

    render(){
        return <p>This is the BuyItemPage page</p>;
    }
}